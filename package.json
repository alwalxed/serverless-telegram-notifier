{
  "name": "telegram-notifier",
  "type": "module",
  "description": "Minimal Hono-based Cloudflare Workers service for forwarding HTTP requests to Telegram",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "hono": "^4.9.4",
    "zod": "^4.1.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "wrangler": "^4.4.0"
  }
}
